# Xiaoye 三步防崩盘 Vibecoding 开发规范 (Development Protocol)

> 本规范的目的不是约束开发者，而是约束 AI 助手。
> 它解决三个致命问题：**人机信息不对等、Agent 与工程不对等、长窗口发散失忆**。

---

## 📏 规范总览

```
┌──────────────────────────────────────────────────────────┐
│           Xiaoye 三步防崩盘开发协议                       │
│                                                          │
│  ┌──────────┐    ┌──────────┐    ┌───────────┐          │
│  │ STEP 1   │───>│ STEP 2   │───>│ STEP 3    │          │
│  │ 对齐     │    │ 执行     │    │ 交接      │          │
│  │ (Align)  │    │ (Code)   │    │ (Handoff) │          │
│  └──────────┘    └──────────┘    └───────────┘          │
│                                                          │
│  解决痛点：      解决痛点：       解决痛点：              │
│  人机信息不对等  Agent与项目      长窗口发散失忆          │
│                  信息不对等                               │
└──────────────────────────────────────────────────────────┘
```

---

## STEP 1：对齐 (Align) — 解决"人与 Agent 信息不对等"

### 问题本质
人类脑中有完整的产品蓝图，但 Agent 只看到了你敲进对话框的那一句话。它不知道你为什么要改这个文件，也不知道这个改动应该服务于哪个更大的目标。

### 强制动作
在每一个冲刺阶段（Milestone）开始前，**必须先创建一份任务清单文件**，而不是直接开始编码。

```
文件命名规范: work_log/TODO_<阶段代号>_<模块名>.md
示例:         work_log/TODO_M1_RAG_SCHEMA.md
```

**任务清单模板**：
```markdown
# M1: RAG Schema 富媒体化升级

## 🎯 本阶段目标 (一句话)
将检索返回体从纯文本升级为带坐标溯源的富媒体结构

## 📋 任务列表
- [ ] 1.1 修改 ES Index Mapping，增加 source_bbox 字段
- [ ] 1.2 修改 Marker-PDF 解析器输出结构，注入图像 URI
- [ ] 1.3 重构 retriever_node 返回 JSON 卡片而非纯字符串
- [ ] 1.4 编写单元测试验证新 Schema 的查询完整性

## 🚫 本阶段的边界 (不要越界)
- 不要动前端代码
- 不要修改 swarm_coordinator.py 的调度逻辑
- 不要引入新的 Python 依赖，除非在此处声明

## ✅ 完成标准 (Definition of Done)
- 全部复选框打勾
- `pytest tests/test_rag_schema.py` 通过
```

### 为什么这能解决问题？
Agent 在编码前被强制要求**先阅读这个文件**。它知道了自己在整棵树上的哪个叶子，知道了边界在哪里，不会去动不该动的东西。

---

## STEP 2：执行 (Code) — 解决"Agent 与项目信息不对等"

### 问题本质
Agent 不知道你的项目里已经有哪些现成的工具函数、哪些约定俗成的命名规范。它只能看到你当次给它开着窗口的那几个文件，然后疯狂地重新发明轮子。

### 强制动作

#### 2a. 阅读项目大总管文件 (CLAUDE.md)
在动手写任何代码之前，Agent 必须先阅读项目根目录下的 `CLAUDE.md`。这个文件记录了：
- 锁定的技术栈版本（绝不偏离）
- 五条硬性禁令（如：禁止子 Agent 招募子 Agent）
- 目录结构约定（新文件放在哪个目录下）
- 代码风格规范（命名、注释、commit 格式）

#### 2b. 编码时的铁律
1. **单文件单职责**：一个 PR 只改一个模块的文件。不要跨模块"顺便"改东西。
2. **先读后写**：在修改任何现有文件之前，必须先完整阅读该文件，理解其现有的上下文和依赖关系。
3. **新函数必须写 docstring**：每个新增函数必须写好参数说明、返回类型和一句话描述。
4. **每完成一个子任务立刻打勾**：回到 `TODO_Mx_xxx.md` 把对应的复选框标记为 `[x]`。

### 为什么这能解决问题？
通过 `CLAUDE.md` 这份"入职手册"，Agent 即使从零开始也能瞬间获得对整个项目的全局认知。而"先读后写"规则则确保它不会在已有的代码上盲人摸象。

---

## STEP 3：交接 (Handoff) — 解决"长窗口发散失忆"

### 问题本质
对话进行到第 50 轮时，AI 开始忘记第 1 轮定下的规矩。更恐怖的是它对自己的遗忘毫无察觉，开始自信满满地胡编变量名和乱删代码。

### 强制动作
**当出现以下任一信号时，必须启动"交接三部曲"：**
1. 当前对话已超过 30 轮
2. 一个阶段（Milestone）的全部复选框已打完勾
3. 你感觉 Agent 开始变得"不对劲"（变慢、重复犯错、答非所问）

**交接三部曲（Antigravity 平台适配版）：**

```
第一步：总结 (Summarize)
   → 让 Agent 将本次会话中的所有改动
     写入 work_log/WORKLOG.md（追加模式）
   → Antigravity 会自动保存 Conversation Log 和 KI，
     但这些是"原始矿石"，WORKLOG 才是"提炼好的金条"

第二步：更新交接指南 (Update Handover)
   → 让 Agent 更新 work_log/HANDOVER_PROMPT.md
     确保它反映出最新的架构状态
   → 如果涉及 Claude Code 源码解析的新发现，
     同步更新对应的 src/xxx/README.md 笔记

第三步：开启新 Antigravity 会话 (Fresh Conversation)
   → 在 Antigravity 中新建一个 Conversation
   → 首条指令：
     "请阅读 @xiaoye/CLAUDE.md 和
      @xiaoye/work_log/HANDOVER_PROMPT.md，
      然后继续执行 @xiaoye/work_log/TODO_Mx_xxx.md
      中未完成的任务。"
   → Antigravity 的 KI 系统会自动携带历史摘要，
     但文件级交接更可靠（KI 可能过期，文件不会）
```

### 为什么这能解决问题？
新窗口的 Agent 拥有**满格的注意力和零污染的上下文**。它通过阅读 `HANDOVER_PROMPT.md` 瞬间继承了上一位的全部工作成果（而不是从混乱的 50 轮对话记忆中去猜）。这就像把一个精力充沛的新员工派上场，同时递给他一份详尽的交班记录——比让一个已经加班 20 小时的熬夜员工继续干活好太多了。

---

## 📎 快速参考卡片

```
┌────────────────────────────────────────────────────────┐
│ 🟢 开始新阶段：                                        │
│    1. 创建 work_log/TODO_Mx_xxx.md                     │
│    2. 让 Agent 阅读 CLAUDE.md + TODO 文件              │
│    3. 开始编码                                         │
│                                                        │
│ 🟡 编码过程中：                                        │
│    • 每完成一个子任务 → 打勾                            │
│    • 遵守 CLAUDE.md 的五条禁令                          │
│    • 先读后写，单文件单职责                              │
│                                                        │
│ 🔴 会话过长或阶段结束：                                 │
│    1. 总结 → WORKLOG.md                                │
│    2. 更新 → HANDOVER_PROMPT.md                        │
│    3. 关窗 → 新开一个全新对话                           │
└────────────────────────────────────────────────────────┘
```
